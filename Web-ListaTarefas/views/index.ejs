<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>To-Do List 📝</title>
  <!-- Bootstrap 5 CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/style.css" />
</head>
<body class="bg-light">

  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-lg-8">

        <!-- Cabeçalho -->
        <div class="text-center mb-4">
          <h1 class="display-5 fw-bold text-primary">Minhas Tarefas</h1>
          <p class="text-muted">Organize suas atividades do dia com simplicidade</p>
        </div>

        <!-- Barra de Progresso -->
        <div class="card shadow-sm mb-4">
          <div class="card-body">
            <h6>Progresso</h6>
            <div class="progress" style="height: 10px;">
              <div class="progress-bar bg-success" role="progressbar" style="width: <%= progress %>%;">
                <%= progress %>% completo
              </div>
            </div>
            <small class="text-muted mt-1 d-block">
              <%= completed %> de <%= total %> tarefas concluídas
            </small>
          </div>
        </div>

        <!-- Formulário de Nova Tarefa -->
        <form action="/add" method="POST" class="mb-4">
          <div class="input-group">
            <input 
              type="text" 
              name="task" 
              class="form-control form-control-lg" 
              placeholder="Adicione uma nova tarefa..." 
              required 
            />
            <button type="submit" class="btn btn-primary btn-lg">➕ Adicionar</button>
          </div>
        </form>

        <!-- Lista de Tarefas -->
        <ul class="list-group mb-4">
          <% if (todos.length === 0) { %>
            <li class="list-group-item text-center text-muted">Nenhuma tarefa cadastrada.</li>
          <% } else { %>
            <% todos.forEach(todo => { %>
              <li class="list-group-item d-flex align-items-center justify-content-between <%= todo.done ? 'bg-light' : '' %>">
                <div class="d-flex align-items-center">
                  <form action="/toggle/<%= todo.id %>" method="POST" style="display: inline;">
                    <button type="submit" class="btn btn-sm <%= todo.done ? 'btn-success' : 'btn-outline-secondary' %> rounded-circle me-3">
                      <%= todo.done ? '✔' : '○' %>
                    </button>
                  </form>
                  <span class="<%= todo.done ? 'text-decoration-line-through text-muted' : '' %>">
                    <%= todo.task %>
                  </span>
                </div>
                <form action="/delete/<%= todo.id %>" method="POST" style="display: inline;">
                  <button type="submit" class="btn btn-sm btn-outline-danger">×</button>
                </form>
              </li>
            <% }); %>
          <% } %>
        </ul>

      </div>
    </div>
  </div>

  <!-- Bootstrap JS (opcional, só se quiser modais, etc) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>